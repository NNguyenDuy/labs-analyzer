version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: labs-analyzer-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: labs_analyzer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: labs-analyzer-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: runner
    container_name: labs-analyzer-api
    restart: on-failure
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/labs_analyzer
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    env_file:
      - backend/.env

  worker:
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: runner
    container_name: labs-analyzer-worker
    command: ["node", "dist/backend/src/worker.js"]
    restart: on-failure
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/labs_analyzer
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    env_file:
      - backend/.env

  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: labs-analyzer-web
    restart: on-failure
    depends_on:
      - api
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:3001
    env_file:
      - frontend/.env.local

volumes:
  postgres_data:
  redis_data:
