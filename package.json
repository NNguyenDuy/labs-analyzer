{
  "name": "labs-analyzer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter labs-analyzer-backend dev\" \"pnpm --filter labs-analyzer-backend worker\" \"pnpm --filter labs-analyzer-frontend dev\"",
    "build": "pnpm --filter labs-analyzer-backend build && pnpm --filter labs-analyzer-frontend build",
    "db:migrate": "pnpm --filter labs-analyzer-backend migrate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.5.3"
  }
}
